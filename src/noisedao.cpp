#include "noisedao.h"

namespace NoiseSimulation {

    NoiseDAO::NoiseDAO()
    {
    }

    NoiseDAO::~NoiseDAO()
    {
    }

    Noise NoiseDAO::load(const std::string& videoFilename) const
    {
        return Noise();
    }

} // namespace NoiseSimulation
